{
  "name": "basic-mcp-server-example",
  "version": "1.0.0",
  "description": "Basic MCP server example using serverless-mcp",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "esbuild index.ts --bundle --platform=node --target=node18 --format=esm --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "serverless-mcp": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "esbuild": "^0.24.2"
  }
}